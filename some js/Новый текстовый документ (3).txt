js

в js к массиву можно обратиться также через объект (var array = new Array(1,2,3);
в js к массиву можно обратиться также через объект(
	menu: {
		dish1 = 'chicken'
	}
	['menu']['chicken']
)

